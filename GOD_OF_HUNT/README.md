# GOD_OF_HUNT

اندیکاتور شکار نقدینگی بر پایهٔ متدولوژی ABCD — ادامهٔ مستقل پروژهٔ
`ABHunter/` (نسخهٔ 2.77) با نام و شماره‌گذاری جدید.

پوشهٔ `ABHunter/` دست‌نخورده می‌ماند و فقط مرجع است. قواعد روش و درس‌های
مهندسی همان‌جا هستند و اینجا هم لازم‌الاجرا:

- [`../ABHunter/METHODOLOGY.md`](../ABHunter/METHODOLOGY.md) — قواعد خود روش ABCD
- [`../ABHunter/LESSONS.md`](../ABHunter/LESSONS.md) — قواعد مهندسی (شبیه‌ساز، زنجیرهٔ نامزد، …)

## فایل‌ها

| فایل | نقش |
|---|---|
| `GOD_OF_HUNT.mq5` | اندیکاتور چارت: رسم AB و چرخهٔ عمر الگو |
| `GOD_OF_HUNT_Core.mqh` | منطق مشترک تشخیص و چرخهٔ عمر |
| `GOD_OF_HUNT_Scanner.mq5` | اسکنر چند نمادی با جدول و نوتیفیکیشن |
| `GOD_OF_HUNT_BT.mq5` | نسخهٔ بک‌تست: فقط بازهٔ تاریخی انتخابی، بدون خاکستری |
| `tools/` | شبیه‌ساز توسعه (`sim` / `diff` / `states`) — در متاتریدر کپی نشود |

پیشوند همهٔ آبجکت‌ها و متغیرهای سراسری از `ABH`/`abh` به `GOH`/`goh` تغییر
کرده، پس GOD_OF_HUNT و ABHunter می‌توانند هم‌زمان روی یک ترمینال نصب باشند
بدون اینکه آبجکت‌ها یا وضعیت هم را خراب کنند. اسکنر GOD_OF_HUNT فقط چارتی را
پیدا می‌کند که اندیکاتور GOD_OF_HUNT رویش نصب است، نه ABHunter.

## الگوها و دکمه های انتخاب

هر الگو در هر دو اندیکاتور یک دکمه تیک دارد؛ خاموش کردنش در اسکنر یعنی
«اسکن نشود» و در اندیکاتور چارت یعنی «رسم نشود». انتخاب در آبجکت مخفی روی
همان چارت ذخیره می‌شود و با تغییر تایم فریم یا نصب دوباره از بین نمی‌رود.

| # | الگو | تشخیص |
|---|---|---|
| 1 | **AB HUNT** | همان ABCD شکار نقدینگی (METHODOLOGY.md) |
| 2 | **INSIDE BAR** | دو کندلی: های و لوی کندل فرزند، های و لوی کندل مادر را تاچ نکرده باشد (مقایسه اکید — تساوی هم رد است). فرزند فقط کندل بسته شده. |
| 3 | **TICK FRACTAL** | سه کندلی، توسعه یافته IB: مادر+فرزند همان شرایط IB، به علاوه مادر حداقل ۶۰٪ بادی و انتهای سویینگ؛ کندل سیگنال حداکثر ۵ کندل بعد از فرزند در جهت سویینگ پشت های/لوی مادر کلوز می‌کند. |

**الگوی منقضی شده روی چارت خاکستری می‌ماند.** هر سه الگو یک رفتار دارند:
وقتی پنجره فعالشان تمام شد، تا انتهای همان روز خاکستری روی چارت می‌مانند تا
بشود بررسی کرد که درست کنار گذاشته شده اند یا نه. الگوی منقضی نوتیفیکیشن
نمی‌فرستد.

**در جدول اسکنر اما پیش فرض حذف می‌شوند** (`ShowDeadInPanel = false` از
1.10)، چون IB و TICK خیلی متراکم تر از AB هستند و تا آخر روز صدها ردیف
خاکستری جمع می‌شد. دو تنظیم جدا هستند: چارت با `ShowDeadPatterns` در
اندیکاتور، جدول با `ShowDeadInPanel` در اسکنر.

**رنگ هر سه الگو رنگ دسته تایم فریم است** (ساختار آبی، تریگر نارنجی، ورود
بنفش): رنگ می‌گوید کدام تایم فریم، شکل می‌گوید کدام الگو — خط AB با لیبل
A/B/C، خطوط افقی با لیبل IB، و دو پاره خط تیک با لیبل TICK.

قواعد INSIDE BAR:

- روی چارت از های و لوی مادر (خط ممتد) و فرزند (خط چین، با
  `IBShowChildLines` قابل خاموش شدن) خط افقی تا `IBLineCandles = 5` کندل
  بعد از فرزند به سمت آینده کشیده می‌شود.
- الگو تا `IBMaxAgeCandles = 5` کندل بعد از فرزند «فعال» است: در جدول اسکنر
  می‌آید و روی چارت رسم می‌شود؛ بعد از آن کنار می‌رود.
- در جدول اسکنر با `STATE = IB` و `DIR = -` می‌آید (الگو جهت ندارد)، در
  فیلتر `FILTER_COK_ONLY` نمی‌آید، و اولین رویت هر الگو یک نوتیفیکیشن
  می‌فرستد (IB گذار وضعیت ندارد).

قواعد TICK FRACTAL:

- **مادر انتهای سویینگ است** (از 1.05 ساده شد): مادر باید در جهت خودش
  افراطی ترین نقطه `TickSwingLookback = 5` کندل قبلش باشد — در سویینگ
  نزولی کف پایین تر از همه، در صعودی سقف بالاتر از همه. قید «۳ کندل هم جهت
  پشت سر هم + شارپ بودن» نسخه های قبل برداشته شد.
- مادر حداقل `TickMotherBodyPercent = 60٪` بادی دارد.
- **جهت شکست هم جهت سویینگ است** (ادامه دهنده): سویینگ نزولی → رد شدن از
  لوی مادر؛ صعودی → رد شدن از های مادر. از 1.06 **کلوز لازم نیست** — به
  محض رد شدن قیمت (حتی فقط با سایه) الگو شکل می‌گیرد — و **کندل در حال
  تشکیل هم می‌تواند سیگنال باشد**. اولین کندلی که در پنجره
  `TickSignalMaxCandles = 5` کندل بعد از فرزند رد شود سیگنال است؛ دیرتر از
  آن الگو بی اعتبار است.
- این repaint نمی‌کند: های و لوی کندل در حال تشکیل هیچ وقت برنمی‌گردد، پس
  الگویی که تشکیل شد ناپدید نمی‌شود. تنها چیزی که تا بسته شدن کندل حرکت
  می‌کند نقطه سوم خط تیک است (سایه ممکن است کشیده تر شود).
- رسم: نزولی از **لوی** مادر → فرزند → سیگنال؛ صعودی از **های** آنها —
  شکل تیک. تا `TickMaxAgeCandles = 5` کندل بعد از سیگنال فعال می‌ماند.
- در جدول اسکنر `STATE = TICK` با `DIR` جهت دار؛ اولین رویت نوتیفیکیشن
  می‌فرستد.

## تست قبل از هر تغییر در منطق تشخیص

```bash
cd GOD_OF_HUNT/tools
python3 mksim.py && python3 mkold.py
g++ -std=c++17 -O0 -w -o /tmp/sim    sim.cpp    && /tmp/sim
g++ -std=c++17 -O0 -w -o /tmp/diff   diff.cpp   && /tmp/diff
g++ -std=c++17 -O0 -w -o /tmp/states states.cpp && /tmp/states
python3 mkcheck.py GOD_OF_HUNT.mq5 GOD_OF_HUNT_Scanner.mq5   # بررسی نحوی (کامپایل واقعی نیست)
```

عدد مرجع `diff` در v1.00: `5097 / 4764 / 1206 / 873` (همان v2.77 مبنا).

## دسته بندی ورودی ها

از 1.09 همه ورودی ها با `input group` دسته بندی شده اند، تا معلوم باشد هر
پارامتر مال کدام الگوست. ترتیب نمایش = ترتیب اعلان، و چون
`GOD_OF_HUNT_Core.mqh` بالای هر دو `.mq5` اینکلود می‌شود، اول دسته های
«تشخیص» می‌آیند و بعد دسته های «رسم»:

| دسته | جا | چه چیزی |
|---|---|---|
| AB HUNT — تشخیص سویینگ | Core | `MinCandles` تا `MaxNonProgressive` |
| AB HUNT — چرخه عمر | Core | `EnableABCD` تا `HideCounterABInRetrace` |
| AB HUNT — کندل شکست | Core | `Break*` |
| INSIDE BAR — تشخیص | Core | `IBMaxAgeCandles` |
| TICK FRACTAL — تشخیص | Core | `Tick*` |
| اندازه پنل روی مانیتور | Core | `UIScalePercent` (فقط ظاهر، در امضای تنظیمات نیست) |
| عمومی (هر سه الگو) | اندیکاتور | رنگ تایم فریم ها، هشدار، الگوی منقضی |
| AB HUNT — رسم | اندیکاتور | خطوط، لیبل ها، FVG، فلش سیگنال |
| INSIDE BAR — رسم | اندیکاتور | `IBLineCandles`، `IBShowChildLines` |
| TICK FRACTAL — رسم و لاگ | اندیکاتور | `TickColor`، `TickLineWidth`، `TickDebug*` |
| نمایشگرهای چارت | اندیکاتور | تایمر، تایم فریم فراکتال، سشن |
| ساعت سشن ها (GMT) | اندیکاتور | هشت ساعت باز/بسته شدن |
| اسکنر — … | اسکنر | دامنه اسکن، نوتیفیکیشن، جدول، کلیک |

دام رایج: `IBMaxAgeCandles` فقط برای INSIDE BAR است و روی TICK FRACTAL اثر
ندارد؛ طول عمر تیک را `TickMaxAgeCandles` تعیین می‌کند.

## اندازهٔ پنل روی مانیتورهای مختلف

مختصات آبجکت های پنل (`XDISTANCE`, `YDISTANCE`, `XSIZE`, `YSIZE`) بر حسب
پیکسل خام است، ولی متاتریدر `OBJPROP_FONTSIZE` را بر حسب «پوینت» می‌گیرد و
با DPI مانیتور به پیکسل تبدیل می‌کند. یعنی روی یک لپ تاپ با اسکیل ۱۵۰ درصد
ویندوز فونت ۱.۵ برابر می‌شود ولی کادر دکمه و فاصلهٔ ردیف ها همان قدر قبلی
می‌ماند — متن دکمه نصفه می‌شود و ردیف های زیر دکمه ها روی هم می‌افتند.

از 1.14 همهٔ اعداد چیدمان از `UiPx()` رد می‌شوند که همان اختلاف را جبران
می‌کند. فونت عمدا ضرب نمی‌شود؛ متاتریدر خودش آن را با DPI بزرگ کرده است.

| ورودی | معنی |
|---|---|
| `UIScalePercent = 0` | خودکار: `TERMINAL_SCREEN_DPI / 96`، محدود به بازهٔ ۱ تا ۳ |
| `UIScalePercent = 150` | دستی، برای وقتی که ترمینال DPI درست نمی‌دهد |

اعداد پایه (اسکیل ۱۰۰ درصد) در `GOD_OF_HUNT_Core.mqh` با ماکروهای `UI_*`
تعریف شده اند: دکمه `140×22`، فاصلهٔ ردیف دکمه ها `30`، فاصلهٔ ردیف های
متنی `20`. ردیف های متنی با `UiTextY()` بعد از شش ردیف دکمه شروع می‌شوند،
پس با اضافه شدن دکمه دیگر دستی جابه‌جا نمی‌شوند.

هندسه عمدا **بیرون** از بلوک `if(ObjectFind(...) < 0)` ست می‌شود تا دکمه ای
که از نصب قبلی روی چارت مانده هم با تغییر مانیتور جابه‌جا و هم‌اندازه شود.

## نسخهٔ بک‌تست — `GOD_OF_HUNT_BT.mq5`

برای بررسی چشمی الگوهای گذشته روی چارت. دقیقا همان اندیکاتور اصلی است با
سه تفاوت:

1. **بازهٔ تاریخی**: `BtFrom` و `BtTo` — فقط الگویی رسم می‌شود که *کندل
   شروعش* در بازه باشد: کندل A برای AB، کندل فرزند برای IB، کندل سیگنال
   برای TICK. `BtTo = 0` یعنی تا انتها. این همان زمانی است که در ستون
   `timeA` فایل‌های `tools/data/patterns_*.csv` نوشته شده.
2. **بدون خاکستری**: الگوی مرده هم با رنگ دستهٔ تایم فریم خودش رسم می‌شود.
   متن علت ابطال کنار خط B می‌ماند. ورودی‌های `ShowDeadPatterns` و
   `DeadPatternColor` در این نسخه وجود ندارند.
3. **فقط همان پنجره از تاریخچه خوانده می‌شود** — نه از `BtFrom` تا امروز.
   عمق تاریخچه ورودی جدا ندارد و از خود بازه حساب می‌شود، با دو حاشیه که
   از ورودی‌های تشخیص می‌آیند: قبل از `BtFrom` به اندازهٔ
   `MaxABSpan + MaxCandles` برای اینکه الگوی ابتدای بازه پیدا شود، و بعد
   از `BtTo` به اندازهٔ `MaxRetraceBars` تا الگوی انتهای بازه فرصت کند
   چرخهٔ عمرش را تمام کند. یعنی انتخاب یک بازهٔ یک‌ماهه از پارسال، فقط
   همان یک ماه را بار می‌کند.

یک برچسب زیر نمایشگرهای چارت نشان می‌دهد چه بازه‌ای بار شده، چند کندل، و
چند الگو رسم شده. اگر قرمز شد یعنی متاتریدر تاریخچهٔ آن بازه را ندارد.

پیشوند همهٔ آبجکت‌هایش `GBT` است (نه `GOH`)، پس می‌شود همزمان با نسخهٔ
زنده روی یک چارت داشتش. اسکنر این را نمی‌بیند و دکمه‌های الگویش با نسخهٔ
زنده همگام نمی‌شوند.

## چنجلاگ

### 1.15 (نسخهٔ بک‌تست 1.05) — گام اول رفع فریز

متاتریدر خودش علت را در تب Experts گزارش کرد:

```
GOD_OF_HUNT (XAUUSD,H4)
indicator is too slow, 3062 ms. rewrite the indicator, please
```

یعنی یک اجرای هندلر **۳ ثانیه** طول کشیده. اندیکاتور روی همان تردی اجرا
می‌شود که چارت قیمت را با آن به‌روز می‌کند، پس در آن مدت کل چارت مرده است —
و چون `OnCalculate` روی هر تیک دوباره صدا زده می‌شود، اجراها روی هم تلنبار
می‌شوند و دیگر جبران نمی‌شود.

اندازه‌گیری با کد و دادهٔ واقعی نشان داد وقت کجا **نمی‌رود**:

| بررسی | عدد | نتیجه |
|---|---|---|
| ریاضیات تشخیص (AB + IB + TICK) | ۰.۰۷ تا ۰.۱۱ میلی‌ثانیه | مقصر نیست |
| تعداد آبجکت روی چارت | بیشینه ۲۴۷ | مقصر نیست |
| فراخوانی API آبجکت در هر بازترسیم کامل | ۲۳۰ تا ۱۴۱۰ | **اینجاست** |

۳۰۶۲ تقسیم بر ~۵۰۰ فراخوانی یعنی حدود **۶ میلی‌ثانیه برای هر فراخوانی**
روی آن دستگاه. روی PC قبلی همان کار حدود ۱۰۰ میلی‌ثانیه بود — زیر آستانهٔ
هشدار و نامرئی. باگ همیشه بود، PC فقط حاشیه داشت.

**این نسخه فقط گام اول است:** `OnCalculate` دیگر روی هر تیک اجرای کامل
نمی‌کند. فقط دو حالت فوری می‌ماند — باز شدن کندل جدید، و بازترسیمی که جای
دیگری درخواست شده (تعویض تایم فریم، دکمه ها). بازترسیم دوره‌ای کار `OnTimer`
است که هر ثانیه اجرا می‌شود. روی XAUUSD در ساعات شلوغ این یعنی از ۲۰ تا ۵۰
اجرا در ثانیه به حدود ۱ اجرا.

همان الگویی است که اسکنر از اول داشت (`OnCalculate` خالی، همه‌چیز روی تایمر).

`lastTickBar` عمدا از `lastBarTime` جداست: دومی داخل `ProcessIndicator`
تصمیم «بازترسیم کامل» را می‌گیرد و اگر مشترک بود، اجرای تایمر شمارنده را جلو
می‌برد و کندل جدید از دید `OnCalculate` گم می‌شد.

سه گام بعدی هنوز اعمال **نشده‌اند** و منتظر نتیجهٔ تست این یکی هستند:
`ObjectMove` به جای حذف و ساخت دوباره (۷ فراخوانی به ازای هر آبجکت → ۲)،
ادغام سه پیمایش جدول آبجکت‌ها در یکی، و ست نکردن خواص ثابت در هر بار.

بدون تغییر در تشخیص: `diff` همان `5097 / 4764 / 1206 / 873` و `states` همان
`0 lost / 0 gained`.

### 1.14 (نسخهٔ بک‌تست 1.04)
- **پنل روی هر مانیتوری اندازهٔ خودش را پیدا می‌کند.** روی لپ تاپ ۱۵ اینچ
  با اسکیل بالای ویندوز، متن دکمه ها نصفه می‌شد («TICK FRACTA») و سه ردیف
  تایمر/فراکتال/سشن روی هم می‌افتادند.
  علت: مختصات پنل پیکسل خام بود ولی متاتریدر `OBJPROP_FONTSIZE` را با DPI
  مانیتور بزرگ می‌کند — فونت بزرگ می‌شد، کادر نه. اندازه‌گیری روی همان
  چیدمان قبلی: تایمر ۱۱ پوینت روی `y=190` و فراکتال روی `y=208` یعنی فقط
  **۳ پیکسل** فاصله در اسکیل ۱۰۰ درصد؛ در ۱۵۰ درصد فونت ۲۲ پیکسل می‌شد و
  ۱۸ پیکسل فاصله جوابگو نبود.
  حالا همهٔ اعداد چیدمان از `UiPx()` رد می‌شوند (`TERMINAL_SCREEN_DPI / 96`،
  محدود به ۱ تا ۳) و با `UIScalePercent` هم می‌شود دستی تعیینش کرد. فونت
  عمدا ضرب نمی‌شود چون متاتریدر خودش بزرگش کرده.
  اعداد پایه هم کمی بازتر شدند: دکمه `120×20` → `140×22`، فاصلهٔ ردیف های
  متنی `18/16` → `20`. ردیف های متنی حالا با `UiTextY()` از روی تعداد
  ردیف دکمه ها حساب می‌شوند، نه با عدد ثابت.
- هندسهٔ دکمه ها از داخل بلوک «فقط موقع ساخت» بیرون آمد، تا دکمه ای که از
  نصب قبلی روی چارت مانده هم با تغییر مانیتور جابه‌جا شود.
- `mksim.py` بلوک چیدمان پنل را از شبیه ساز حذف می‌کند (بین دو نشانهٔ
  `UI-LAYOUT-BEGIN/END`)؛ ربطی به تشخیص ندارد.
- بدون تغییر در تشخیص: `diff` همان `5097 / 4764 / 1206 / 873` و `states`
  همان `0 lost / 0 gained`.

### 1.13 (نسخهٔ بک‌تست 1.03)
- **رفع باگ شمارندهٔ برچسب**: تعداد الگو اول درست نشان داده می‌شد و
  بلافاصله صفر می‌شد، در حالی که الگوها روی چارت بودند. علت: شمارش IB و
  TICK داخل بلوک `fullRedraw` بود، پس فقط در اولین اجرا انجام می‌شد و از
  تیک بعدی تایمر — که بازترسیم کامل لازم ندارد — از قلم می‌افتاد. شمارش
  حالا مستقل از رسم انجام می‌شود.

### 1.13
- **رفع دو باگ نسخهٔ بک‌تست** که در تست پیدا شدند:
  - روی تایم فریم **ورود** فقط یک الگو رسم می‌شد. علت: نسخهٔ بک‌تست همان
    `keepOnlyLast` نسخهٔ زنده را پاس می‌داد که برای دستهٔ ورود روشن است.
    در زنده عمدی است (شلوغ نشدن چارت)، در بک‌تست یعنی از کل بازه یک الگو.
    حالا برای هر سه دسته خاموش است.
  - **INSIDE BAR و TICK تقریبا هیچ الگویی نشان نمی‌دادند.** `ScanAllHistory`
    فقط شروع پیمایش را باز می‌کرد، ولی داخل حلقه قید «الگوی منقضی فقط اگر
    امروز منقضی شده باشد» باقی مانده بود و بقیه را دور می‌ریخت. حالا آن قید
    هم با `KeepAllDeadPatterns` برداشته می‌شود.
- سناریوی تست برای همین حالت به `sim.cpp` اضافه شد: روی یک سری شش‌روزه،
  حالت عادی ۲ الگو و حالت بک‌تست هر ۱۰ الگو را برمی‌گرداند.

### 1.12
- **بازهٔ بک‌تست حالا خودش عمق تاریخچه را تعیین می‌کند.** ورودی
  `BtHistoryBars` حذف شد: به‌جای خواندن N کندل آخر (که برای بازهٔ یک سال
  پیش یعنی بار کردن یک سال کندل تا امروز)، فقط پنجرهٔ خود بازه به‌علاوهٔ دو
  حاشیهٔ محاسبه‌شده خوانده می‌شود.
- در هسته `AnalyzeSymbolRange` اضافه شد که داده را از یک بازهٔ زمانی
  می‌گیرد. بدنهٔ مشترک با `AnalyzeSymbol` به `AnalyzeLoaded` منتقل شد تا دو
  پیاده‌سازی موازی وجود نداشته باشد؛ رفتار `AnalyzeSymbol` عوض نشده —
  diff و states بدون تغییر.
- برچسب وضعیت روی چارت نسخهٔ بک‌تست: بازهٔ بارشده، تعداد کندل، تعداد الگوی
  رسم‌شده؛ قرمز اگر تاریخچه موجود نباشد.

### 1.11
- **نسخهٔ بک‌تست `GOD_OF_HUNT_BT.mq5`** اضافه شد (بالا توضیح داده شده).
- در هسته دو کلید سراسری اضافه شد که **ورودی نیستند** و فقط نسخهٔ بک‌تست
  روشنشان می‌کند: `KeepAllDeadPatterns` (الگوی مرده صرف‌نظر از تاریخ نگه
  داشته شود) و `ScanAllHistory` (تشخیص IB و TICK کل تاریخچه را بگردد).
  پیش‌فرض هر دو خاموش، پس رفتار اندیکاتور زنده و اسکنر ذره‌ای عوض نشده و
  اثر انگشت تنظیمات هم دست‌نخورده است — با diff و states تایید شد.
- `mkcheck.py` لیترال تاریخ `D'...'` را می‌شناسد.

### 1.10
- **ردیف خاکستری از جدول اسکنر برداشته شد** (`ShowDeadInPanel = false`).
  الگوی باطل/منقضی روی چارت خاکستری می‌ماند ولی جدول را شلوغ نمی‌کند.
  همین ورودی ردیف های «از لیست افتاده» را هم کنترل می‌کند تا جدول یکدست
  باشد؛ `ExpiredKeepSeconds` وقتی معنا دارد که این روشن باشد.
- **رنگ TICK FRACTAL هم رنگ دسته تایم فریم شد** (مثل AB و INSIDE BAR).
  ورودی `TickColor` که سرخابی ثابت بود حذف شد — روی تریگر و ورود با رنگ
  دسته اشتباه گرفته می‌شد. حالا هر سه الگو یک قاعده رنگ دارند و تفکیکشان
  با شکل است.

### 1.09
- **دسته بندی همه ورودی ها با `input group`** بر اساس الگو (جدول بالا).
  دو الگوی جدید پارامترهای هم نام زیاد دارند و در فهرست تخت قابل تفکیک
  نبودند.
- پیش فرض `TickMotherBodyPercent` از ۵۰ به **۶۰**. این آستانه را
  **سخت گیرتر** می‌کند: طبق سنجش، فراوانی از ۱ در ۴۹ به حدودا ۱ در ۱۰۱
  کندل می‌رود (نصف).
- `ShowDeadPatterns`/`DeadPatternColor` به دسته «عمومی» منتقل شدند، چون از
  1.05 هر سه الگو از آنها استفاده می‌کنند نه فقط AB.
- `mkcheck.py` و `mksim.py` خط `input group` را نادیده می‌گیرند.

### 1.08
- **لاگ تشخیصی TICK FRACTAL** (`TickDebugLog`، پیش فرض خاموش): برای هر
  Inside Bar در محدوده اسکن می‌نویسد که چرا تیک نشد — یا اگر شد، زمان
  کندل سیگنالش. خروجی در تب **Experts** متاتریدر. `TickDebugBars = 200`
  عمق لاگ را محدود می‌کند تا روی تایم فریم پایین پر نشود.
  نمونه خروجی:

  ```
  --- GOD_OF_HUNT tick log  XAUUSD H3  (body>=50.0%  lookback=5  signal<=5 bars)
  IB child 2026.08.06 00:00  mother 2026.08.05 21:00  BULL  body 62.4%
     -> mother is not the swing extreme
     (mother 4282.10, blocked by 4285.40 at -2 bars)
  ```

- بازآرایی هسته: شرط های تیک به `EvaluateTickCandidate` منتقل شدند که
  علاوه بر قبول/رد، **کد علت** و جزئیاتش را برمی‌گرداند. هم تشخیص و هم
  لاگ از همین یک تابع استفاده می‌کنند، پس نمی‌توانند از هم واگرا شوند.
  `TickSwingEnd` که حالا کپی دوم همان قاعده بود حذف شد.
- چهار سناریوی تست برای کدهای علت به `sim.cpp` اضافه شد.

### 1.07
- **رفع باگ**: با خاموش کردن یک الگو، آبجکت هایش روی چارت باقی می‌ماند.
  `ProcessIndicator` فقط دسته و تایم فریم جاری را پاک می‌کند و آبجکت تایم
  فریم های بالاتر عمدا نگه داشته می‌شود (کار فراکتالی)، پس خطوطی که در
  تایم فریم دیگری رسم شده بودند پاک نمی‌شدند. حالا
  `DeleteOffPatternObjects` آبجکت های الگوی خاموش را در **همه** دسته ها و
  تایم فریم ها پاک می‌کند — موقع کلیک دکمه، موقع همگام شدن با اسکنر، و یک
  بار در `OnInit` برای ته مانده نصب قبلی.
- تفکیک آبجکت هر الگو از روی نشانه نام (`L`/`C` برای AB، `IB`، `TK`) که
  بعد از `<cat>_<tf>_` می‌آید.

### 1.06
- **آپدیت فوری موقع نصب**: اندیکاتور چارت حالا در `OnInit` رسم می‌کند و
  منتظر اولین تیک بازار نمی‌ماند. اسکنر از قبل در `OnInit` اسکن می‌کرد ولی
  تاریخچه هنوز دانلود نشده بود و جدول تا یک دوره تایمر خالی می‌ماند؛ حالا
  تا وقتی داده ناقص است هر ۳ ثانیه دوباره اسکن می‌کند (سقف ۴۰ بار) و بعد
  به `RefreshSeconds` برمی‌گردد. شمارش معکوس سربرگ همین فاصله موثر را
  نشان می‌دهد.
- همراهش یک باگ بالقوه بسته شد: «اسکن اول» حالا اولین اسکنی است که داده اش
  کامل بوده. قبلا اگر اسکن اول بی داده بود، لحظه دانلود شدن تاریخچه انبوه
  نوتیفیکیشن از الگوهای قدیمی می‌آمد.
- **قاعده سیگنال TICK عوض شد**: کلوز پشت های/لوی مادر لازم نیست — به محض
  رد شدن قیمت (حتی با سایه) الگو شکل می‌گیرد — و کندل در حال تشکیل هم
  می‌تواند سیگنال باشد. repaint نمی‌کند (توضیح بالا).
- اثر بر فراوانی (۹۰هزار کندل تصادفی، بادی ۵۰٪، lookback=5): از ۱ در ۵۷
  به **۱ در ۴۹** کندل — حدودا ۱۶٪ بیشتر.

### 1.05
- **الگوی IB و TICK منقضی شده دیگر ناپدید نمی‌شود**: مثل الگوی مرده AB تا
  انتهای همان روز خاکستری روی چارت و ته جدول می‌ماند (`X old`)، بدون
  نوتیفیکیشن. فیلد `expired` + `deadTime` به هر دو ساختار اضافه شد و
  پیمایش تشخیص تا اول روز جاری عقب می‌رود.
- **قاعده سویینگ TICK ساده شد**: به جای «۳ کندل هم جهت پشت سر هم + دو شرط
  شارپ بودن»، فقط «مادر اکسترمم `TickSwingLookback = 5` کندل قبلش باشد».
  ورودی های `TickSwingMinCandles` و `TickSwingBodyPercent` حذف و
  `TickSwingLookback` جایشان آمد.
- سنجش فراوانی با قواعد جدید (۹۰هزار کندل تصادفی، بدون فیلتر سن):
  با بادی ۵۰٪ و lookback=5 حدودا یک الگو در هر ۵۷ کندل — در برابر یک در
  هر ۳۴۶۱ کندلِ نسخه 1.02. اثر خود ساده سازی سویینگ حدودا ۱٫۷ برابر است و
  بقیه از کاهش آستانه بادی می‌آید. اگر شلوغ شد،
  `TickMotherBodyPercent = 60` تقریبا نصفش می‌کند.
- `AnalyzeInsideBars`/`AnalyzeTickFractals` حالا `ABCDHistoryBars` کندل
  می‌گیرند تا خاکستری های امروز را هم پیدا کنند.

### 1.04
- پیش فرض `TickMotherBodyPercent` از ۸۰ به **۵۰** (درخواست صاحب پروژه، بعد
  از دیدن آمار فراوانی نسخه 1.03). طبق سنجش، حدودا ۳۵ برابر الگوی بیشتر.
- یادآوری متاتریدر: مقدار پیش فرض جدید فقط بعد از حذف و افزودن دوباره
  اندیکاتور اعمال می‌شود؛ جایگزین کردن فایل و کامپایل کافی نیست
  (LESSONS.md بند ۳).

### 1.03
- **همگام سازی دکمه های الگو** بین چارت اندیکاتور و اسکنر
  (`SyncPatternsWithChart`): خاموش کردن الگو روی هر کدام، روی دیگری هم
  اعمال می‌شود — مرجع، آبجکت وضعیت چارت اندیکاتور است و هر دو سمت هر ثانیه
  با آن همراه می‌شوند. محدودیت: اگر اسکنر روی پروفایل جدا باشد چارت
  اندیکاتور دیده نمی‌شود و دکمه های خود اسکنر مرجع می‌مانند.
- **رنگ Inside Bar به رنگ دسته تایم فریم** (ساختار آبی، تریگر نارنجی،
  ورود بنفش — مثل AB) تا سطوح تایم فریم های مختلف که روی هم می‌افتند قابل
  تفکیک باشند. ورودی `IBColor` حذف شد.
- بررسی «هیچ TICK پیدا نشد»: باگ نبود — قیف شرایط روی ۹۰هزار کندل تصادفی:
  ۷۶۵۸ اینساید بار → ۱۰۹ با مادر ≥۸۰٪ بادی → ۷۷ سویینگ شارپ → ۲۶ سیگنال
  در پنجره ۵ کندلی ≈ یک الگو در هر ۳۵۰۰ کندل؛ و اسکنر فقط ۵ کندل آخر را
  فعال می‌داند. گلوگاه اصلی بادی ۸۰٪ مادر است (۷۰٪ → حدودا ۶ برابر، ۵۰٪ →
  ۳۵ برابر). قواعد عمدا دست نخوردند.

### 1.02
- الگوی سوم **TICK FRACTAL**: تشخیص در هسته (`CollectTickFractals` +
  `TickSharpSwing`)، رسم دو پاره خط مادر→فرزند→سیگنال در اندیکاتور، ردیف
  `TICK` جهت دار و نوتیفیکیشن در اسکنر. دکمه تیک سوم در هر دو اندیکاتور
  خودکار از `PATTERN_COUNT` ساخته شد.
- دو تصمیم تفسیری (با ورودی قابل تغییر): تعریف کمی «شارپ» و «شکست هم جهت
  سویینگ» — بالا مستند شده اند.
- ورودی های `Tick*` مؤثر بر تشخیص وارد اثر انگشت تنظیمات شدند؛
  `TickColor` و `TickLineWidth` فقط رسم اند و نه.
- برچسب های تایمر/فراکتال/سشن یک پله دیگر پایین رفتند (190/208/224).
- چهار سناریوی تست TICK به `sim.cpp` اضافه شد (تشخیص درست + سه قاعده رد).

### 1.01
- الگوی جدید **INSIDE BAR**: تشخیص در هسته (`CollectInsideBars`)، رسم خطوط
  های و لوی مادر و فرزند در اندیکاتور، ردیف `IB` و نوتیفیکیشن در اسکنر.
- **دکمه های تیک الگو** در هر دو اندیکاتور (AB HUNT و INSIDE BAR)، با
  ذخیره انتخاب روی خود چارت. الگوی خاموش شده در اسکنر ردیف خاکستری هم
  نمی‌گیرد — با انتخاب کاربر رفته، نه با ابطال.
- رفع باگ همراه: کلیک روی ردیف جدول اسکنر (PointHunterTF) فیلدهای اضافه
  آبجکت وضعیت اندیکاتور را می‌انداخت و از این نسخه دست نخورده برمی‌گرداند.
- برچسب های تایمر/فراکتال/سشن اندیکاتور برای جا دادن دکمه ها پایین تر رفتند.
- `IBMaxAgeCandles` وارد اثر انگشت تنظیمات شد؛ ورودی های صرفا رسمی
  (`IBLineCandles`، `IBShowChildLines`) عمدا نه.
- بازسازی شیم بررسی نحوی داخل ریپو: `tools/mkcheck.py` + `tools/mql5_stub.h`
  (قبلا در scratchpad جلسه بود و از بین رفت). سناریوی تست IB به `sim.cpp`
  اضافه شد.

### 1.00
- شروع پروژه: کپی کامل ABHunter v2.77 با نام GOD_OF_HUNT.
- تغییر همهٔ پیشوندهای آبجکت/متغیر سراسری (`ABH_→GOH_`، `abhst_→gohst_`،
  `ABHscan_→GOHscan_`، …) برای هم‌زیستی با نصب ABHunter.
- ابزارهای تست به `GOD_OF_HUNT/tools` منتقل و به هستهٔ جدید وصل شدند؛ هر سه
  تست با نتیجهٔ مرجع v2.77 سبز.
- هیچ تغییری در منطق تشخیص یا چرخهٔ عمر نیست.
